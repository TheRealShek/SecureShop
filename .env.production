# For LOCAL testing on Windows:
DATABASE_URL=your_actual_supabase_database_url
SUPABASE_JWT_SECRET=your_actual_jwt_secret
ALLOWED_ORIGINS=http://localhost,http://localhost:3000,http://localhost:5173

# For LOCAL testing:
VITE_API_URL=http://localhost:8080
VITE_SUPABASE_URL=https://wxspzkuevywwglbwjggy.supabase.co
VITE_SUPABASE_ANON_KEY=your_actual_anon_key

## Commands to run after adding the prod keys

# Step 1: Build the containers (takes 5-10 minutes first time)
# docker compose --env-file .env.production build

# Step 2: Run the containers
# docker compose --env-file .env.production up

#Step 3: Test in browser

#Frontend: http://localhost
#Backend health: http://localhost:8080/api/healthz

#Step 4: Stop (when done)
# Press Ctrl+C, then:
#docker compose down
